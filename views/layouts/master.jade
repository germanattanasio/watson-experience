doctype html
html(lang='en')
  head
    block title
      title Cognitive Experience
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='shortcut icon', href='images/favicon.ico', type='image/x-icon')
    link(rel='icon', href='images/favicon.ico', media='all', type='image/x-icon')
    link(rel='stylesheet', href='css/theme.css', media='screen')
    script.
      var logged_in = #{locals.session.id ? true : false}; //TODO: check session here
      var applications = !{JSON.stringify(locals.applications)};
    if locals.session.id
      script var current_user = !{JSON.stringify(locals.session)};
    else
      script var current_user = null;

  body
    #app
      block content
    script(src='js/theme.js')
    if ga
      //- Using Google Analytics
      script.
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', '#{ga}', 'auto');
        ga('send', 'pageview');